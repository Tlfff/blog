# 微服务实践

本文档记录博客系统在阶段二“从 DDD 单体演进为三个微服务”中实际落地的服务化设计。内容按**服务**组织：统一入口、Identity Service、Content Service、Community Service，并补充共享契约、共享平台、服务间通信、数据所有权、治理、部署与回滚，便于按部署单元阅读与核对。

> **阅读约定**
> - 「内部 gRPC」指 `shared/contracts` 定义、用于服务间同步调用的接口；「开放 gRPC」指 `proto/blogopen/v1` 定义、对二方/三方暴露的接口。
> - 「代码位置」以仓库根目录为基准，标注了文件名的条目可直接在对应文件中核对。
> - 阶段二仍处于共享 MySQL 阶段；物理拆库、真实基础设施联调与生产流量切换见第 11 节演进项。

## 1. 总览

阶段二把阶段一形成的三个领域边界抽取为三个独立服务进程，并保留统一入口作为边缘适配层：

```text
统一入口（blog-server / blog-grpc）
        │ 内部 gRPC
        ├────────────┬─────────────┬─────────────┐
        ▼            ▼             ▼             ▼
  Identity Service  Content Service  Community Service
        │            │             │
        │            │             └── Kafka 消费者（通知/浏览历史）
        └────────────┴─────────────┴── 共享 MySQL / MongoDB / Redis / Kafka / MinIO
```

| 单元 | 目录 | 职责 |
| --- | --- | --- |
| 统一入口 | `cmd/server.go`、`cmd/grpc.go` | HTTP/开放 gRPC 协议适配、鉴权、转发内部 gRPC |
| Identity Service | `services/identity` | 用户、认证、会话、资料、头像 |
| Content Service | `services/content` | 文章生命周期、列表、详情、图片 |
| Community Service | `services/community` | 评论、点赞、浏览、热榜、通知与 Kafka 消费者 |

每个服务内部继续保留阶段一的四层依赖结构（Interfaces -> Application -> Domain，Infrastructure 实现 Port），领域与应用层代码仍复用 `internal/` 中的共享模块。

> 数据访问已收敛为标准形态：各服务基础设施 Adapter 直接持有 GORM/MongoDB 实现领域 Port，横向 `internal/repository` 已移除；跨上下文防腐通过内部 gRPC Client 完成。

## 2. 服务划分与依赖图

```text
统一入口 ──gRPC──▶ Identity Service
   │                  ▲
   │ gRPC             │ GetUserBasicInfo
   ▼                  │
Content Service ──gRPC─┤
   │                  │
   │ gRPC             │
   ▼                  │
Community Service ────┘
   │
   └──gRPC──▶ Content Service（GetArticleInfo）
```

| 调用方 | 被调用方 | 内部接口 | 用途 |
| --- | --- | --- | --- |
| 统一入口 | Identity | `IdentityService` | 注册、登录、退出、资料、改密、头像、用户查询 |
| 统一入口 | Content | `ContentService` | 文章 CRUD、列表、详情、图片凭证 |
| 统一入口 | Community | `CommunityService` | 评论、点赞、浏览、热榜、通知 |
| Content | Identity | `GetUserBasicInfo` | 文章详情作者信息 |
| Content | Community | `IsUserLikedArticle` | 文章详情点赞状态 |
| Community | Identity | `GetUserBasicInfo` | 通知发送方公开信息 |
| Community | Content | `GetArticleInfo` | 通知消费所需的文章作者/标题 |

## 3. 共享契约（shared/contracts）

### 3.1 内部 gRPC

代码位置：`shared/contracts/proto/internal/v1/`，生成代码在 `shared/contracts/gen/internalv1/`。

| Proto | 服务 | 主要方法 |
| --- | --- | --- |
| `identity.proto` | `IdentityService` | `Register`、`Login`、`Logout`、`GetMyProfile`、`GetPublicProfile`、`UpdateProfile`、`VerifyOldPassword`、`ChangePassword`、`UpdatePassword`、`UpdateAccount`、`GetAvatarUploadURL`、`ConfirmAvatar`、`GetUserBasicInfo`、`ListUserBasicInfo` |
| `content.proto` | `ContentService` | `CreateArticle`、`UpdateArticle`、`DeleteArticle`、`ClearArticle`、`PublishArticle`、`RecoverArticle`、`GetPublishedArticle`、`GetArticle`、`GetPublishedList`、`GetAdminList`、`GetAvailableList`、`GetImageUploadURL`、`GetArticleInfo` |
| `community.proto` | `CommunityService` | `CreateComment`、`ListRootComments`、`ListReplies`、`DeleteComment`、`GetCommentStats`、`ArticleLike`、`ArticleCancelLike`、`CommentLike`、`CommentCancelLike`、`IsUserLikedArticle`、`GetHotRank`、`RebuildHotRank`、`SendViewHistory`、`GetNotifications`、`GetUnreadCount`、`ClearUnread` |

公共消息（`Empty`、`UserIDRequest`、`ArticleUserRequest`、`UploadURLResponse`）放在 `common.proto`，避免同包重复定义。

### 3.2 开放 proto 兼容

开放 gRPC 继续使用 `proto/blogopen/v1` 与根目录 `gen/`，内部 gRPC 独立演进；统一入口的开放 gRPC Handler 改为调用内部 gRPC Client，保持方法名、字段和鉴权语义不变。

### 3.3 Kafka 事件信封

代码位置：`shared/contracts/events/`。

```json
{
  "event_id": "uuid",
  "event_type": "notification",
  "version": "v1",
  "occurred_at": "RFC3339",
  "payload": {}
}
```

- `envelope.go`：构造、序列化、解码与 payload 解码。
- `notification.go` / `view_history.go`：通知与浏览历史事件载荷。
- 事件类型：`notification`、`view_history`；版本：`v1`。

## 4. 共享平台（shared/platform）

| 包 | 职责 | 代码位置 |
| --- | --- | --- |
| `config` | 服务级配置：name/id/enabled/GRPCAddr/超时/Redis 前缀/Kafka group/Peers | `shared/platform/config/service.go` |
| `errors` | 业务错误与 gRPC code 双向映射 | `shared/platform/errors/errors.go` |
| `trace` | Trace ID 生成 | `shared/platform/trace/trace.go` |
| `grpc` | 客户端 Dial（注入 service id + trace + 超时）、服务端身份校验拦截器 | `shared/platform/grpc/{client,server}.go` |
| `log` | 统一日志辅助 | `shared/platform/log/log.go` |
| `server` | gRPC 服务启动、health、优雅退出 | `shared/platform/server/server.go` |

## 5. 服务实现

### 5.1 Identity Service

代码位置：`services/identity/`。

- 启动入口：`main.go`，连接 MySQL、Redis、MinIO，组装 `internal/application/identity.Service`。
- 内部 gRPC 实现：`services/identity/internal/grpc/handler.go`。
- 服务级配置：`config.yaml`（`grpc_addr: 127.0.0.1:9101`）。
- 部署：`services/identity/Dockerfile`、`deploy/k8s/43-blog-identity.yaml`。

### 5.2 Content Service

代码位置：`services/content/`。

- 启动入口：`main.go`，连接 MySQL、MinIO，通过 gRPC 调用 Identity（作者信息）与 Community（点赞状态）。
- 内部 gRPC 实现：`services/content/internal/grpc/handler.go`。
- 服务级配置：`config.yaml`（`grpc_addr: 127.0.0.1:9102`，peers 指向 identity/community）。
- 部署：`services/content/Dockerfile`、`deploy/k8s/44-blog-content.yaml`。

### 5.3 Community Service

代码位置：`services/community/`。

- 启动入口：`main.go`，连接 MySQL、MongoDB、Redis、Kafka，通过 gRPC 调用 Identity 与 Content，并启动 Kafka 消费者（通知/浏览历史）。
- 内部 gRPC 实现：`services/community/internal/grpc/handler.go`。
- 服务级配置：`config.yaml`（`grpc_addr: 127.0.0.1:9103`，独立 Kafka consumer group）。
- 部署：`services/community/Dockerfile`、`deploy/k8s/45-blog-community.yaml`。

### 5.4 统一入口

代码位置：`cmd/server.go`、`cmd/grpc.go`。

- HTTP：路由、中间件、鉴权保持在 `internal/interfaces/http/`；业务调用全部改为内部 gRPC Client。
- 开放 gRPC：`internal/interfaces/grpc/handler/` 保持开放接口，底层改为内部 gRPC Client。
- 统一入口不再连接 MySQL、MongoDB、Kafka、MinIO，仅保留 Redis 用于会话鉴权与 HMAC nonce。
- 内部 gRPC Client 位于 `internal/interfaces/grpc/client/`。

## 6. 服务间通信与防腐

### 6.1 同步调用

| 调用方 | 防腐接口 | Client 实现 |
| --- | --- | --- |
| 统一入口 | `UserAuthUsecase`/`UserUsecase`/`UserQueryUsecase` | `client.IdentityClient` |
| 统一入口 | `ArticleUsecase`/`ArticleQueryUsecase` | `client.ContentClient` |
| 统一入口 | `CommentUsecase`/点赞/通知/热榜/浏览 | `client.CommunityClient` |
| Content | `domaincontent.UserQuery` | `client.ContentUserQueryClient` |
| Content | `domaincontent.ArticleInteractionQuery` | `client.ContentInteractionClient` |
| Community | `domaincommunity.UserInfoQuery` | `client.CommunityUserInfoClient` |
| Community | `domaincommunity.ArticleQuery.FindByID` | `client.CommunityArticleQueryClient`（热榜列表仍用本地只读适配器） |

### 6.2 异步调用

- 浏览历史：统一入口经 `SendViewHistory` 发送 Kafka 事件，Community Service 消费者落库并自增计数。
- 通知：点赞主流程发送 `notification` 事件，Community Service 消费者生成通知。
- Community Service 使用独立的 consumer group（`blog_community_notification_consumer`、`blog_community_view_consumer`），避免与兼容入口争抢。

## 7. 数据所有权

代码位置：`docs/architecture/data-ownership-enforcement.md`。

| 服务 | 写/全部访问 | 只读访问 |
| --- | --- | --- |
| Identity | `users`、Redis `auth:*`/`user:*`、MinIO `avatar/*` | 无 |
| Content | `articles` 基础字段、MinIO `article/*` | Identity 作者信息、Community 点赞状态 |
| Community | `comments`、`article_likes`、`comment_likes`、`article_view_histories`、MongoDB `notifications`、Redis `like:*`/`rank:*` | Content 文章信息、Identity 用户信息 |
| 统一入口 | 无 | Redis（会话鉴权、nonce） |

## 8. 服务治理

代码位置：`docs/architecture/service-governance.md`。

- 服务身份：`x-service-id` metadata + 服务端允许列表校验。
- 超时：客户端统一 3 秒默认超时，不允许无限等待。
- Trace：`x-trace-id` 跨服务透传。
- 错误映射：`shared/platform/errors.ToGRPC` / `FromGRPC` 保持 HTTP 错误码兼容。
- Kafka：事件信封含 `event_id/version`；重复消息用 `event_id` 幂等，未知版本进入死信。
- 重试/死信：沿用 Kafka 基础设施配置，临时错误重试 3 次，超限写 `dead_letter`。

## 9. 部署

### 9.1 Docker Compose

`docker-compose.yml` 新增 `blog-identity`、`blog-content`、`blog-community`，统一入口 `blog-server`/`blog-grpc` 依赖三服务；中间件沿用 `mysql/redis/mongodb/kafka`。

### 9.2 Kubernetes

- `deploy/k8s/43-blog-identity.yaml`
- `deploy/k8s/44-blog-content.yaml`
- `deploy/k8s/45-blog-community.yaml`

每个清单包含 ConfigMap（服务级配置）、ClusterIP Service、Deployment 与 TCP 探针；业务配置继续来自 `blog-config` Secret。

### 9.3 启动顺序

1. 基础设施（MySQL/Redis/MongoDB/Kafka/MinIO）。
2. Identity Service -> Content Service -> Community Service。
3. 统一入口 `server` / `grpc`。

## 10. 路由开关与回滚

代码位置：`docs/architecture/routing-switch-and-rollback.md`。

```yaml
# services/identity/config.yaml 等
enabled: true
```

- `enabled: true`：统一入口通过内部 gRPC 调用该服务。
- `enabled: false`：统一入口拒绝启动，避免请求打入未就绪服务。
- 回滚：先停新服务写请求，再回退统一入口镜像；旧单体 `blog-server` 镜像保留 `server`/`grpc`/`kafka-consume` 命令作为回滚目标。

## 11. 验证与演进

### 11.1 已完成的验证

| 检查项 | 结果 |
| --- | --- |
| `go test ./...` | 通过 |
| `go vet ./...` | 通过 |
| `go build ./...` | 通过 |
| `make check-arch` | 通过 |
| `docker compose config` | 通过 |
| 内部 gRPC 契约生成 | 通过 |
| 服务身份拦截器测试 | 通过 |
| Identity/Content/Community 客户端契约测试 | 通过 |
| Kafka 事件信封测试 | 通过 |

### 11.2 演进项

- 在真实基础设施上执行端到端启动与探活（当前环境未联调）。
- 物理拆库：完成备份、回放校验与迁移演练后，把 `articles` 统计列迁移到 Community 读模型。
- Kafka 消费者逐步迁移到统一信封，并完善死信观测与重放治理。
- `cmd/kafka-consume` 保留为兼容入口，生产环境以 Community Service 承载为准。
