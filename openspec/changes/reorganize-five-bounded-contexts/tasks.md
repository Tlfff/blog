## 1. 上下文与目录边界

- [x] 1.1 建立 `user`、`article`、`comment`、`like`、`notification` 五个上下文的四层目录与包文档
- [x] 1.2 明确各上下文的聚合根、实体、值对象、领域事件和 Port，禁止跨上下文共享聚合实体
- [x] 1.3 将现有 Identity、Content、Community 代码按上下文迁移，并保留必要的兼容适配器

## 2. User 与 Article 上下文

- [x] 2.1 将用户认证、会话、资料和用户查询 Port 迁移到 `user` 上下文
- [x] 2.2 将文章生命周期、公开查询、图片存储和文章查询 Port 迁移到 `article` 上下文
- [x] 2.3 更新 `identity`、`content` 服务装配，使其分别承载 User 和 Article 上下文

## 3. Comment 上下文

- [x] 3.1 将评论、回复、删除权限和评论统计规则迁移到 `comment` 上下文
- [x] 3.2 为 Comment 定义文章存在性/可评论性查询 Port，并通过防腐适配器访问 Article
- [x] 3.3 保持现有评论 HTTP/gRPC 路由、鉴权语义和响应契约兼容

## 4. Like 上下文

- [x] 4.1 将文章点赞和评论点赞迁移到 `like` 上下文，统一目标类型、状态转换和幂等规则
- [x] 4.2 为 Like 定义目标验证、点赞记录、缓存和统计 Port，禁止直接依赖 Article/Comment Repository
- [x] 4.3 发布版本化的 `LikeCreated`、`LikeCanceled` 事件，并实现重复事件和重复请求安全处理
- [x] 4.4 保持点赞记录自身强一致，验证计数、热榜和通知链路的最终一致行为

## 5. Notification 上下文

- [x] 5.1 将通知聚合、通知查询、未读状态和通知基础设施迁移到 `notification` 上下文
- [x] 5.2 实现点赞、评论和回复事件的通知消费者，并按事件 ID 保证幂等
- [x] 5.3 增加失败重试、死信和消费可观测性，确保通知失败不回滚主业务
- [x] 5.4 保持通知列表、未读统计、一键已读接口和权限语义兼容

## 6. Community 服务装配与跨上下文协作

- [x] 6.1 更新 `community` 服务，使其在同一进程内装配 Comment、Like、Notification 三个上下文
- [x] 6.2 为同步目标验证和统计查询设置超时、错误映射与服务身份校验
- [x] 6.3 将点赞/评论到通知的协作切换为事件契约，将非核心统计联动切换为事件或查询接口
- [x] 6.4 更新统一 HTTP、开放 gRPC、内部 gRPC 和 Kafka 入口的路由映射与依赖方向

## 7. 读模型与数据所有权

- [x] 7.1 将热榜读模型归属 Article 上下文并从 Article 入口装配
- [x] 7.2 评估文章互动摘要是否需要独立统计读模型，仅在高频聚合查询场景启用
- [x] 7.3 明确各上下文对用户、文章、评论、点赞和通知数据的逻辑写入所有权
- [x] 7.4 保持数据库表结构兼容，禁止跨上下文直接写入对方领域数据

## 8. 验证与清理

- [x] 8.1 更新领域、应用、接口、基础设施和服务契约测试
- [x] 8.2 验证重复点赞、重复事件、目标不存在、通知延迟、服务超时和死信场景
- [x] 8.3 运行架构检查，确认领域层不依赖技术框架且跨上下文无直接 Repository 依赖
- [x] 8.4 删除旧 Community 聚合入口和迁移期间不再使用的兼容代码
