## 1. 领域模型与端口

- [x] 1.1 在 Article 聚合中增加初始化空草稿的领域构造行为，保持普通创建和编辑的标题、正文非空校验，并补充作者与草稿状态测试
- [x] 1.2 增加文章图片上传权限的领域校验行为，覆盖作者、非作者和已删除文章场景
- [x] 1.3 调整 `ArticleImageStorage` Port，增加按文章对象前缀删除图片的能力，并保持对象存储实现细节不进入 Domain

## 2. 应用层用例

- [x] 2.1 增加初始化文章用例，创建空内容草稿并返回 `article_id`，补充 Repository 失败和正常路径测试
- [x] 2.2 定义批量图片凭证的 Application 输入输出结构，每个文件包含 `client_id` 和 `file_ext`，每个结果返回对应上传 URL 与正式 URL
- [x] 2.3 实现批量凭证用例：加载文章、执行领域权限校验、整体校验扩展名、生成 `article/<article_id>/<uuid>.<ext>` 对象 Key 并逐项签发凭证
- [x] 2.4 补充批量凭证测试，覆盖不同扩展名、非法扩展名整批失败、非作者、已删除文章、对象存储错误和正式路径格式
- [x] 2.5 调整硬删除应用编排，在领域校验通过后先清理文章图片前缀，再物理删除文章记录
- [x] 2.6 补充硬删除测试，覆盖空目录成功、图片清理失败时保留数据库记录、图片成功后数据库删除失败以及重试路径

## 3. Infrastructure 对象存储适配

- [x] 3.1 在 MinIO Client 中实现递归列出并删除指定前缀对象，正确处理列表错误、单对象删除错误、上下文取消和空前缀结果
- [x] 3.2 在 Article 图片存储适配器中实现按 `article/<article_id>/` 前缀清理，并补充适配器或 MinIO 可替代测试
- [x] 3.3 删除 `MoveObject`、`PromoteImages` 和旧临时上传路径，并补充新流程直接保存正式 URL 的回归测试

## 4. HTTP 接口与路由

- [x] 4.1 增加文章初始化请求处理和响应 DTO，返回 `article_id`，并注册管理员私有路由
- [x] 4.2 增加批量图片凭证请求和响应 DTO，完成字段校验、当前用户传递和 Application 结果转换
- [x] 4.3 注册批量图片凭证路由，并删除旧单张临时凭证路由及请求处理
- [x] 4.4 更新 Handler 和路由契约测试，覆盖初始化成功、批量请求校验、权限错误及响应字段映射

## 5. 调用流程与兼容文档

- [x] 5.1 更新接口和架构文档，说明创建按钮执行“初始化、批量获取凭证、并发上传、等待全部成功、调用更新接口”的顺序
- [x] 5.2 记录更新按钮只上传新增本地图片，正文移除图片、保存草稿、发表和软删除均不清理 OSS 对象
- [x] 5.3 记录硬删除按 `article/<article_id>/` 前缀清理的失败与重试语义，以及页面关闭会中断未完成上传的限制
- [x] 5.4 更新兼容性基线，明确旧单张临时上传接口和图片转正逻辑已经移除

## 6. 格式化与验证

- [x] 6.1 对本次修改的全部 Go 文件执行 `gofmt`
- [x] 6.2 执行 Article Domain、Application、Infrastructure 和 HTTP 受影响包测试
- [x] 6.3 执行 `go test ./...`，如受既有环境依赖阻塞则记录未执行项、原因和风险
- [x] 6.4 检查最终 diff，确认未修改生成文件、未引入数据库结构变更、敏感信息或无关格式调整
