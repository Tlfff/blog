## 1. 图片数据模型与持久化

- [x] 1.1 新增 `article_images` 建表脚本，包含可空 `article_id`、唯一 `object_key`、创建时间和 `article_id` 普通索引。
- [x] 1.2 在 Article 上下文新增图片领域对象、持久化模型和 Repository Port，支持创建未绑定图片、按 ID 批量查询及按文章 ID 查询。
- [x] 1.3 扩展图片 Repository Port，支持批量绑定、批量解绑、按文章 ID 删除图片记录，并要求条件更新返回实际影响行数。
- [x] 1.4 实现 GORM 图片 Repository，复用事务上下文，并覆盖创建回填、空集合、缺失记录、绑定冲突和条件更新测试。

## 2. 单图片实时上传

- [x] 2.1 将批量上传命令和响应 DTO 替换为单图片请求及 `image_id、upload_url、url` 响应结构。
- [x] 2.2 实现扩展名校验、`article/img/<year>/<month>/<uuid>.<ext>` Key 生成、预签名 URL 获取和未绑定图片记录创建流程。
- [x] 2.3 将私有路由从 `POST /article/image/upload-urls` 替换为 `POST /article/image/upload-url`，移除 `article_id` 和 `client_id` 参数。
- [x] 2.4 更新 Application 与 HTTP Adapter 测试，覆盖合法扩展名、非法扩展名、对象存储失败、数据库失败和响应字段映射。

## 3. 正文图片引用与详情映射

- [x] 3.1 实现基于 Goldmark 图片节点的 `image://<正整数>` 引用提取器，对图片 ID 去重并忽略普通文本、代码和非图片链接。
- [x] 3.2 为引用提取器增加表驱动测试，覆盖重复引用、非法 ID、历史完整 URL、外部 URL、代码块和空正文。
- [x] 3.3 扩展公开详情与管理员编辑详情 DTO，返回原始 `content` 和 `images` 图片 ID—URL 列表。
- [x] 3.4 在详情查询中按图片 ID 和当前文章 ID 批量读取图片记录并拼接公开 URL，缺失或归属不符时省略对应映射。
- [x] 3.5 更新详情 Application 和 HTTP 测试，覆盖多图片、重复图片、域名切换、未绑定图片、错误归属及无占位符正文。

## 4. 文章保存与图片关系事务

- [x] 4.1 将现有本地事务协调器注入 Article Application，并使 Article Repository 使用上下文中的事务连接。
- [x] 4.2 调整创建文章流程，在同一事务内创建文章、校验正文图片均未绑定并将其绑定到新文章。
- [x] 4.3 调整更新文章流程，比较新旧正文图片 ID，在同一事务内更新正文、绑定新增图片并解绑移除图片。
- [x] 4.4 对不存在、已绑定其他文章或条件更新数量不符的图片引用返回错误，并保证文章和图片关系全部回滚。
- [x] 4.5 增加创建和更新测试，覆盖无图片、全部新增、保留旧图、增删混合、非法图片、绑定冲突和事务回滚。

## 5. 生命周期与硬删除

- [x] 5.1 删除 `POST /article/init` 路由、初始化请求处理、Application 用例、响应 DTO 及相关测试。
- [x] 5.2 保持文章软删除和恢复期间的图片绑定关系不变，并增加对应回归测试。
- [x] 5.3 调整硬删除流程，按文章 ID 查询图片，先逐个清理 OSS 对象，再在数据库事务中删除图片记录和文章记录。
- [x] 5.4 更新对象存储 Port 和适配器以支持按对象 Key 删除，移除仅用于目录前缀清理的能力。
- [x] 5.5 更新硬删除测试，覆盖无图片、全部清理成功、对象清理失败、数据库删除失败和幂等重试语义。

## 6. 组装、文档与验证

- [x] 6.1 在 Article 模块组装图片 Repository、引用解析器和事务协调器，更新构造函数、测试替身及依赖注入。
- [x] 6.2 更新文章图片接口文档和前端调用说明，明确 PUT 成功后写入占位符、保存时同步归属及详情按 `images` 映射渲染。
- [x] 6.3 明确记录未绑定图片定时清理不在本次范围内，避免实现任务扩张到孤立图片回收。
- [x] 6.4 对本次修改的 Go 文件执行 `gofmt`，运行受影响包测试和 `go test ./...`，记录无法执行的环境依赖及风险。
- [x] 6.5 检查最终 diff，确认未修改生成文件、未引入上传者权限、媒体库或跨文章图片复用能力。
